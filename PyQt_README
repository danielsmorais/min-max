* Instalação do QtPy5:

	sudo apt-get install PyQt5
	sudo apt-get install pyqt5-dev-tools

	sudo apt-get install python3-pyqt5
	sudo apt-get install python3-pyqt5.qtsql	#Opcional

* Depois que fazer a interface com o Qt Designer, executar 

	pyuic5 -x mainwindow.ui -o main.py

* Executa apenas o arquivo em python 

	python main.py



